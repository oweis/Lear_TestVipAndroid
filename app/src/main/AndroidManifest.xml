<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.LearBIP">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:debuggable="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name="com.example.user.LearBIP.DisplayFamilys"
            android:theme="@style/splashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DisplayCables" />
        <activity android:name="com.example.user.LearBIP.DisplayWires" />
        <activity android:name=".DisplayConnectors"></activity>
        <activity android:name="com.example.user.LearBIP.DisplaySucces"></activity>
    </application>

</manifest>